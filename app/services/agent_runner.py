# This is where you can move your notebook code
# Define it as a function to reuse in FastAPI

def run_agent(prompt: str) -> str:
    # Replace this logic with your real agent
    return f"Agent received: {prompt}"
